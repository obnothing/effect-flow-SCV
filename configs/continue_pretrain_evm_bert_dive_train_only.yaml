experiment_name: continue_pretrain_evm_bert_dive_train_only
model_type: evm_bert_base
tokenizer_type: evm

base_hf_model_path: checkpoints/pretrain_evm_bert_base_full_bjut/hf_model
vocab_path: data/processed/BJUT_SC01/evm_vocab.json
pretrain_corpus_path: data/processed/DIVE/pretrain_corpus_dive_train_only.jsonl

checkpoint_dir: checkpoints/continue_pretrain_evm_bert_dive_train_only
result_dir: results/continue_pretrain_evm_bert_dive_train_only
report_dir: data/reports
log_dir: logs

use_labels: false
is_dive_transductive: false
is_transductive_pretraining: false
continued_from: pretrain_evm_bert_base_full_bjut

max_len: 512
chunk_content_size: 510
chunk_stride: 510
max_chunks_per_contract: 16
mlm_probability: 0.15

batch_size: 16
gradient_accumulation_steps: 8
effective_batch_size: 128

learning_rate: 0.00002
weight_decay: 0.01
adam_beta1: 0.9
adam_beta2: 0.999
adam_epsilon: 0.00000001
warmup_ratio: 0.06
epochs: 3
max_grad_norm: 1.0
seed: 42
num_workers: 4
fp16: true
bf16: false
logging_steps: 100

save_every_epoch: true
save_best_mlm_loss: true
